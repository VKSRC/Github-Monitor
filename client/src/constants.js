const API_HOST = 'http://10.32.16.99:8000';

const leakageStatus = ['未处理', '已处理', '白名单'];

const leakageTagColor = ['blue', '#666', 'green'];

const taskStatus = ['等待中', '运行中', '完成'];

export { API_HOST, leakageStatus, leakageTagColor, taskStatus };
